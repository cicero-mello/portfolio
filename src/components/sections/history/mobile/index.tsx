import { component$ } from "@builder.io/qwik"
import * as S from "./styles.css"

export const Mobile = component$(() => {

    return (
        <S.MainSection>
            Mobile History
        </S.MainSection>
    )
})

import { component$ } from "@builder.io/qwik"
import * as S from "./styles.css"

export const QuestionSection = component$(() => {

    return (
        <S.Section>
            (Question)
        </S.Section>
    )
})
